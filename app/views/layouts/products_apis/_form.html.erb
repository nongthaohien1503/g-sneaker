<%= form_with(model: products_api) do |form| %>
  <% if products_api.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(products_api.errors.count, "error") %> prohibited this products_api from being saved:</h2>

      <ul>
        <% products_api.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
